
const initialState = {
    isLogged: false,
    role: ''
};

const authReducer = ( state = initialState, action) => {
    switch(action.type){
        case :
        return 

        default:
            return state
    }
};

export default authReducer;